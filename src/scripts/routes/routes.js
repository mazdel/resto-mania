import Home from '../views/pages/home';
import Favorite from '../views/pages/favorite';

const routes = {
  '/': Home,
  '/home': Home,
  '/favorite': Favorite,
  // '/detail/:id': Detail,
  // '/like': Like,
};

export default routes;
